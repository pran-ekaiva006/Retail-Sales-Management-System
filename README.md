# Retail Sales Management System

Full‑stack app with Search, Filters, Sorting, Pagination (10/page).

## Run
Backend: `cd backend && npm i && npm run dev`

Frontend: `cd frontend && npm i && npm run dev`
